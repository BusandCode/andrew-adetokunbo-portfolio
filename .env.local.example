# Sanity CMS (optional — site falls back to lib/data.ts until these are set)
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Resend (required for the contact form to actually send email)
RESEND_API_KEY=
